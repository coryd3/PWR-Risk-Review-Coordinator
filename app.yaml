command: ["sh", "-c", "echo 'Running DB migrations...' && npx drizzle-kit push --force --config ./lib/db/drizzle.config.ts && echo 'Migrations complete. Starting app...' && NODE_ENV=production exec node --enable-source-maps artifacts/api-server/dist/index.mjs"]

resources:
  - name: pwr-risk-review-db
    lakebase:
      project: pwr-risk-review-coordinator
      branch: production
      database: databricks_postgres
      permission: read_write
