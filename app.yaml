command: ["sh", "-c", "NODE_ENV=production exec node --enable-source-maps artifacts/api-server/dist/index.mjs"]

resources:
  - name: pwr-risk-review-db
    lakebase:
      project: pwr-risk-review-coordinator
      branch: production
      database: databricks_postgres
      permission: read_write
