openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: PWR Risk Review Coordinator API
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: requests
    description: Risk review requests
  - name: meetings
    description: Meetings
  - name: email-drafts
    description: Email drafts
  - name: rules
    description: Rules and classification
  - name: notes
    description: Notes
  - name: status
    description: Status workflow
  - name: calendar
    description: Calendar previews
  - name: dashboard
    description: Dashboard summaries
  - name: config
    description: Configuration and admin
paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      description: Returns server health status
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  /requests:
    get:
      operationId: listRequests
      tags: [requests]
      summary: List all risk review requests
      responses:
        "200":
          description: List of requests
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/RiskReviewRequest"
    post:
      operationId: createRequest
      tags: [requests]
      summary: Create a risk review request
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RiskReviewRequestInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RiskReviewRequestDetail"

  /requests/{id}:
    get:
      operationId: getRequest
      tags: [requests]
      summary: Get a request with full detail
      parameters:
        - $ref: "#/components/parameters/RequestId"
      responses:
        "200":
          description: Request detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RiskReviewRequestDetail"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      operationId: updateRequest
      tags: [requests]
      summary: Update a request
      parameters:
        - $ref: "#/components/parameters/RequestId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RiskReviewRequestUpdate"
      responses:
        "200":
          description: Updated request detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RiskReviewRequestDetail"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      operationId: deleteRequest
      tags: [requests]
      summary: Delete a request
      parameters:
        - $ref: "#/components/parameters/RequestId"
      responses:
        "204":
          description: Deleted
        "404":
          $ref: "#/components/responses/NotFound"

  /requests/{id}/classify:
    post:
      operationId: classifyRequest
      tags: [rules]
      summary: Re-run classification and validation for a request
      parameters:
        - $ref: "#/components/parameters/RequestId"
      responses:
        "200":
          description: Classification result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClassificationResult"
        "404":
          $ref: "#/components/responses/NotFound"

  /requests/{id}/meetings:
    get:
      operationId: listRequestMeetings
      tags: [meetings]
      summary: List meetings for a request
      parameters:
        - $ref: "#/components/parameters/RequestId"
      responses:
        "200":
          description: Meetings
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Meeting"
    post:
      operationId: createMeeting
      tags: [meetings]
      summary: Add a meeting to a request
      parameters:
        - $ref: "#/components/parameters/RequestId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MeetingInput"
      responses:
        "201":
          description: Created meeting
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Meeting"

  /meetings:
    get:
      operationId: listMeetings
      tags: [meetings]
      summary: List all meetings
      responses:
        "200":
          description: Meetings
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/MeetingWithRequest"

  /meetings/{id}:
    put:
      operationId: updateMeeting
      tags: [meetings]
      summary: Update a meeting
      parameters:
        - $ref: "#/components/parameters/MeetingId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MeetingUpdate"
      responses:
        "200":
          description: Updated meeting
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Meeting"
        "404":
          $ref: "#/components/responses/NotFound"

  /requests/{id}/email-drafts:
    get:
      operationId: listEmailDrafts
      tags: [email-drafts]
      summary: List email drafts for a request
      parameters:
        - $ref: "#/components/parameters/RequestId"
      responses:
        "200":
          description: Email drafts
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EmailDraft"

  /requests/{id}/email-drafts/generate:
    post:
      operationId: generateEmailDrafts
      tags: [email-drafts]
      summary: Generate editable email drafts for a request
      parameters:
        - $ref: "#/components/parameters/RequestId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GenerateDraftsInput"
      responses:
        "201":
          description: Generated drafts
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EmailDraft"

  /email-drafts/{id}:
    put:
      operationId: updateEmailDraft
      tags: [email-drafts]
      summary: Update an email draft
      parameters:
        - $ref: "#/components/parameters/EmailDraftId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EmailDraftUpdate"
      responses:
        "200":
          description: Updated draft
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmailDraft"
        "404":
          $ref: "#/components/responses/NotFound"

  /requests/{id}/notes:
    get:
      operationId: listNotes
      tags: [notes]
      summary: List notes for a request
      parameters:
        - $ref: "#/components/parameters/RequestId"
      responses:
        "200":
          description: Notes
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Note"
    post:
      operationId: createNote
      tags: [notes]
      summary: Add a note to a request
      parameters:
        - $ref: "#/components/parameters/RequestId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NoteInput"
      responses:
        "201":
          description: Created note
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Note"

  /requests/{id}/status:
    post:
      operationId: changeStatus
      tags: [status]
      summary: Change request status and record history
      parameters:
        - $ref: "#/components/parameters/RequestId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StatusChangeInput"
      responses:
        "200":
          description: Updated request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RiskReviewRequest"
        "404":
          $ref: "#/components/responses/NotFound"

  /requests/{id}/status-history:
    get:
      operationId: listStatusHistory
      tags: [status]
      summary: List status history for a request
      parameters:
        - $ref: "#/components/parameters/RequestId"
      responses:
        "200":
          description: Status history
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/StatusHistoryEntry"

  /requests/{id}/calendar-preview:
    post:
      operationId: generateCalendarPreview
      tags: [calendar]
      summary: Generate calendar invite preview data (no real event created)
      parameters:
        - $ref: "#/components/parameters/RequestId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CalendarPreviewInput"
      responses:
        "200":
          description: Calendar preview
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CalendarPreview"
        "404":
          $ref: "#/components/responses/NotFound"

  /risk-triggers:
    get:
      operationId: listRiskTriggers
      tags: [rules]
      summary: List master risk triggers
      responses:
        "200":
          description: Risk triggers
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/RiskTrigger"

  /rule-sets:
    get:
      operationId: listRuleSets
      tags: [rules]
      summary: List configurable rule sets
      responses:
        "200":
          description: Rule sets
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/RuleSet"

  /email-templates:
    get:
      operationId: listEmailTemplates
      tags: [config]
      summary: List email templates
      responses:
        "200":
          description: Email templates
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EmailTemplate"

  /risk-triggers/{id}:
    put:
      operationId: updateRiskTrigger
      tags: [rules]
      summary: Update a risk trigger
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RiskTriggerUpdate"
      responses:
        "200":
          description: Updated risk trigger
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RiskTrigger"
        "404":
          $ref: "#/components/responses/NotFound"

  /email-templates/{id}:
    put:
      operationId: updateEmailTemplate
      tags: [config]
      summary: Update an email template
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EmailTemplateUpdate"
      responses:
        "200":
          description: Updated email template
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmailTemplate"
        "404":
          $ref: "#/components/responses/NotFound"

  /rule-sets/{id}:
    put:
      operationId: updateRuleSet
      tags: [rules]
      summary: Update a rule set
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RuleSetUpdate"
      responses:
        "200":
          description: Updated rule set
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RuleSet"
        "404":
          $ref: "#/components/responses/NotFound"

  /config:
    get:
      operationId: getConfig
      tags: [config]
      summary: Get configuration options (enums and static rules)
      responses:
        "200":
          description: Configuration
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppConfig"

  /dashboard/summary:
    get:
      operationId: getDashboardSummary
      tags: [dashboard]
      summary: Get dashboard summary card counts
      responses:
        "200":
          description: Dashboard summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardSummary"

  /usage:
    get:
      operationId: listUsageEvents
      tags: [usage]
      summary: List recent usage-tracking events
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
      responses:
        "200":
          description: Recent usage events
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/UsageEvent"
    post:
      operationId: recordUsageEvent
      tags: [usage]
      summary: Record a usage-tracking event (open to any platform)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RecordUsageBody"
      responses:
        "201":
          description: Created usage event
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UsageEvent"

  /usage/summary:
    get:
      operationId: getUsageSummary
      tags: [usage]
      summary: Aggregated usage impact (hours and dollars saved)
      responses:
        "200":
          description: Usage impact summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UsageSummary"

  /import/tracker:
    post:
      operationId: importTracker
      tags: [config]
      summary: Import the legacy risk-review tracker spreadsheet
      description: >
        Upload a legacy .xlsx/.xls/.csv tracker export and run the same staging
        and import logic as the CLI importer. Use dryRun=true to preview what
        would be imported without writing any data.
      parameters:
        - name: dryRun
          in: query
          required: false
          description: When true, classify and report without writing any data.
          schema:
            type: boolean
            default: false
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/TrackerUpload"
      responses:
        "200":
          description: Import summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ImportSummary"
        "400":
          description: Invalid upload or unreadable file
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

components:
  parameters:
    RequestId:
      name: id
      in: path
      required: true
      schema:
        type: integer
    MeetingId:
      name: id
      in: path
      required: true
      schema:
        type: integer
    EmailDraftId:
      name: id
      in: path
      required: true
      schema:
        type: integer
  responses:
    NotFound:
      description: Resource not found
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status
    Error:
      type: object
      properties:
        error:
          type: string
      required:
        - error

    RiskTrigger:
      type: object
      required:
        - id
        - triggerNumber
        - triggerName
        - isMajorOpportunityTrigger
        - active
      properties:
        id:
          type: integer
        triggerNumber:
          type: integer
        triggerName:
          type: string
        triggerDescription:
          type: ["string", "null"]
        isMajorOpportunityTrigger:
          type: boolean
        active:
          type: boolean

    Attendee:
      type: object
      required:
        - id
        - requestId
        - role
      properties:
        id:
          type: integer
        requestId:
          type: integer
        name:
          type: ["string", "null"]
        email:
          type: ["string", "null"]
        role:
          type: string
        attendeeType:
          type: ["string", "null"]
        source:
          type: ["string", "null"]
        isRequired:
          type: boolean

    AttendeeInput:
      type: object
      required:
        - role
      properties:
        name:
          type: ["string", "null"]
        email:
          type: ["string", "null"]
        role:
          type: string
        attendeeType:
          type: ["string", "null"]
        source:
          type: ["string", "null"]
        isRequired:
          type: boolean

    Meeting:
      type: object
      required:
        - id
        - requestId
        - meetingType
        - status
        - rescheduledCount
      properties:
        id:
          type: integer
        requestId:
          type: integer
        meetingType:
          type: string
        targetDate:
          type: ["string", "null"]
        scheduledStart:
          type: ["string", "null"]
        scheduledEnd:
          type: ["string", "null"]
        timezone:
          type: ["string", "null"]
        subject:
          type: ["string", "null"]
        body:
          type: ["string", "null"]
        teamsLink:
          type: ["string", "null"]
        outlookEventId:
          type: ["string", "null"]
        status:
          type: string
        riskLead:
          type: ["string", "null"]
        rescheduledCount:
          type: integer
        notes:
          type: ["string", "null"]

    MeetingWithRequest:
      type: object
      required:
        - id
        - requestId
        - meetingType
        - status
        - rescheduledCount
      properties:
        id:
          type: integer
        requestId:
          type: integer
        meetingType:
          type: string
        targetDate:
          type: ["string", "null"]
        scheduledStart:
          type: ["string", "null"]
        scheduledEnd:
          type: ["string", "null"]
        timezone:
          type: ["string", "null"]
        subject:
          type: ["string", "null"]
        body:
          type: ["string", "null"]
        teamsLink:
          type: ["string", "null"]
        outlookEventId:
          type: ["string", "null"]
        status:
          type: string
        riskLead:
          type: ["string", "null"]
        rescheduledCount:
          type: integer
        notes:
          type: ["string", "null"]
        projectName:
          type: ["string", "null"]
        clientName:
          type: ["string", "null"]
        crmOpportunityNumber:
          type: ["string", "null"]
        requiredAttendees:
          type: array
          items:
            type: string
        optionalAttendees:
          type: array
          items:
            type: string

    MeetingInput:
      type: object
      required:
        - meetingType
      properties:
        meetingType:
          type: string
        targetDate:
          type: ["string", "null"]
        scheduledStart:
          type: ["string", "null"]
        scheduledEnd:
          type: ["string", "null"]
        timezone:
          type: ["string", "null"]
        subject:
          type: ["string", "null"]
        body:
          type: ["string", "null"]
        teamsLink:
          type: ["string", "null"]
        status:
          type: ["string", "null"]
        riskLead:
          type: ["string", "null"]
        notes:
          type: ["string", "null"]

    MeetingUpdate:
      type: object
      properties:
        meetingType:
          type: string
        targetDate:
          type: ["string", "null"]
        scheduledStart:
          type: ["string", "null"]
        scheduledEnd:
          type: ["string", "null"]
        timezone:
          type: ["string", "null"]
        subject:
          type: ["string", "null"]
        body:
          type: ["string", "null"]
        teamsLink:
          type: ["string", "null"]
        status:
          type: string
        riskLead:
          type: ["string", "null"]
        rescheduledCount:
          type: integer
        notes:
          type: ["string", "null"]

    EmailTemplate:
      type: object
      required:
        - id
        - templateName
        - templateType
        - subjectTemplate
        - bodyTemplate
        - active
      properties:
        id:
          type: integer
        templateName:
          type: string
        templateType:
          type: string
        appliesToMajor:
          type: ["boolean", "null"]
        appliesToBusinessLine:
          type: ["string", "null"]
        appliesToRequestType:
          type: ["string", "null"]
        subjectTemplate:
          type: string
        bodyTemplate:
          type: string
        active:
          type: boolean

    EmailDraft:
      type: object
      required:
        - id
        - requestId
        - subject
        - body
        - status
      properties:
        id:
          type: integer
        requestId:
          type: integer
        meetingId:
          type: ["integer", "null"]
        templateId:
          type: ["integer", "null"]
        templateType:
          type: ["string", "null"]
        toRecipients:
          type: string
        ccRecipients:
          type: string
        subject:
          type: string
        body:
          type: string
        status:
          type: string
        createdAt:
          type: ["string", "null"]
        updatedAt:
          type: ["string", "null"]
        sentAt:
          type: ["string", "null"]

    EmailDraftUpdate:
      type: object
      properties:
        toRecipients:
          type: string
        ccRecipients:
          type: string
        subject:
          type: string
        body:
          type: string
        status:
          type: string

    GenerateDraftsInput:
      type: object
      properties:
        templateTypes:
          type: array
          items:
            type: string
        meetingId:
          type: ["integer", "null"]

    RuleSet:
      type: object
      required:
        - id
        - name
        - priority
        - active
      properties:
        id:
          type: integer
        name:
          type: string
        conditionJson:
          type: ["string", "null"]
        outputJson:
          type: ["string", "null"]
        priority:
          type: integer
        active:
          type: boolean

    RiskTriggerUpdate:
      type: object
      properties:
        triggerName:
          type: string
        triggerDescription:
          type: ["string", "null"]
        isMajorOpportunityTrigger:
          type: boolean
        active:
          type: boolean

    EmailTemplateUpdate:
      type: object
      properties:
        templateName:
          type: string
        subjectTemplate:
          type: string
        bodyTemplate:
          type: string
        active:
          type: boolean

    RuleSetUpdate:
      type: object
      properties:
        name:
          type: string
        conditionJson:
          type: ["string", "null"]
        outputJson:
          type: ["string", "null"]
        priority:
          type: integer
        active:
          type: boolean

    StatusHistoryEntry:
      type: object
      required:
        - id
        - requestId
        - newStatus
      properties:
        id:
          type: integer
        requestId:
          type: integer
        previousStatus:
          type: ["string", "null"]
        newStatus:
          type: string
        changedBy:
          type: ["string", "null"]
        changedAt:
          type: ["string", "null"]
        notes:
          type: ["string", "null"]

    Note:
      type: object
      required:
        - id
        - requestId
        - noteText
      properties:
        id:
          type: integer
        requestId:
          type: integer
        noteText:
          type: string
        createdBy:
          type: ["string", "null"]
        createdAt:
          type: ["string", "null"]

    NoteInput:
      type: object
      required:
        - noteText
      properties:
        noteText:
          type: string
        createdBy:
          type: ["string", "null"]

    ValidationWarning:
      type: object
      required:
        - code
        - message
      properties:
        code:
          type: string
        message:
          type: string

    ClassificationResult:
      type: object
      required:
        - isMajorOpportunity
        - businessLineClassification
        - warnings
      properties:
        isMajorOpportunity:
          type: boolean
        businessLineClassification:
          type: string
        warnings:
          type: array
          items:
            $ref: "#/components/schemas/ValidationWarning"

    RiskReviewRequest:
      type: object
      required:
        - id
      properties:
        id:
          type: integer
        createdAt:
          type: ["string", "null"]
        updatedAt:
          type: ["string", "null"]
        requesterName:
          type: ["string", "null"]
        requesterEmail:
          type: ["string", "null"]
        clientName:
          type: ["string", "null"]
        projectName:
          type: ["string", "null"]
        crmOpportunityNumber:
          type: ["string", "null"]
        bmcdContractValueRaw:
          type: ["string", "null"]
        bmcdContractValueNumeric:
          type: ["number", "null"]
        totalInstalledCostRaw:
          type: ["string", "null"]
        totalInstalledCostNumeric:
          type: ["number", "null"]
        businessLines:
          type: array
          items:
            type: string
        businessLineClassification:
          type: ["string", "null"]
        contractReviewRvwNumber:
          type: ["string", "null"]
        isEpcPrime:
          type: boolean
        isMajorOpportunity:
          type: boolean
        requestType:
          type: ["string", "null"]
        riskIdentificationStatus:
          type: ["string", "null"]
        preRiskTargetDate:
          type: ["string", "null"]
        formalRiskTargetDate:
          type: ["string", "null"]
        proposalDueDate:
          type: ["string", "null"]
        formalRiskDiscussionDate:
          type: ["string", "null"]
        finalRiskTargetDate:
          type: ["string", "null"]
        preRiskLead:
          type: ["string", "null"]
        formalRiskLead:
          type: ["string", "null"]
        status:
          type: ["string", "null"]
        nextAction:
          type: ["string", "null"]
        owner:
          type: ["string", "null"]
        notes:
          type: ["string", "null"]

    RiskReviewRequestDetail:
      type: object
      required:
        - id
      properties:
        id:
          type: integer
        createdAt:
          type: ["string", "null"]
        updatedAt:
          type: ["string", "null"]
        requesterName:
          type: ["string", "null"]
        requesterEmail:
          type: ["string", "null"]
        clientName:
          type: ["string", "null"]
        projectName:
          type: ["string", "null"]
        crmOpportunityNumber:
          type: ["string", "null"]
        bmcdContractValueRaw:
          type: ["string", "null"]
        bmcdContractValueNumeric:
          type: ["number", "null"]
        totalInstalledCostRaw:
          type: ["string", "null"]
        totalInstalledCostNumeric:
          type: ["number", "null"]
        businessLines:
          type: array
          items:
            type: string
        businessLineClassification:
          type: ["string", "null"]
        contractReviewRvwNumber:
          type: ["string", "null"]
        isEpcPrime:
          type: boolean
        isMajorOpportunity:
          type: boolean
        requestType:
          type: ["string", "null"]
        riskIdentificationStatus:
          type: ["string", "null"]
        preRiskTargetDate:
          type: ["string", "null"]
        formalRiskTargetDate:
          type: ["string", "null"]
        proposalDueDate:
          type: ["string", "null"]
        formalRiskDiscussionDate:
          type: ["string", "null"]
        finalRiskTargetDate:
          type: ["string", "null"]
        preRiskLead:
          type: ["string", "null"]
        formalRiskLead:
          type: ["string", "null"]
        status:
          type: ["string", "null"]
        nextAction:
          type: ["string", "null"]
        owner:
          type: ["string", "null"]
        notes:
          type: ["string", "null"]
        triggers:
          type: array
          items:
            $ref: "#/components/schemas/RiskTrigger"
        attendees:
          type: array
          items:
            $ref: "#/components/schemas/Attendee"
        meetings:
          type: array
          items:
            $ref: "#/components/schemas/Meeting"
        warnings:
          type: array
          items:
            $ref: "#/components/schemas/ValidationWarning"

    RiskReviewRequestInput:
      type: object
      properties:
        requesterName:
          type: ["string", "null"]
        requesterEmail:
          type: ["string", "null"]
        clientName:
          type: ["string", "null"]
        projectName:
          type: ["string", "null"]
        crmOpportunityNumber:
          type: ["string", "null"]
        bmcdContractValueRaw:
          type: ["string", "null"]
        totalInstalledCostRaw:
          type: ["string", "null"]
        businessLines:
          type: array
          items:
            type: string
        contractReviewRvwNumber:
          type: ["string", "null"]
        isEpcPrime:
          type: boolean
        requestType:
          type: ["string", "null"]
        riskIdentificationStatus:
          type: ["string", "null"]
        preRiskTargetDate:
          type: ["string", "null"]
        formalRiskTargetDate:
          type: ["string", "null"]
        proposalDueDate:
          type: ["string", "null"]
        formalRiskDiscussionDate:
          type: ["string", "null"]
        finalRiskTargetDate:
          type: ["string", "null"]
        preRiskLead:
          type: ["string", "null"]
        formalRiskLead:
          type: ["string", "null"]
        status:
          type: ["string", "null"]
        nextAction:
          type: ["string", "null"]
        owner:
          type: ["string", "null"]
        notes:
          type: ["string", "null"]
        triggerIds:
          type: array
          items:
            type: integer
        attendees:
          type: array
          items:
            $ref: "#/components/schemas/AttendeeInput"

    RiskReviewRequestUpdate:
      type: object
      properties:
        requesterName:
          type: ["string", "null"]
        requesterEmail:
          type: ["string", "null"]
        clientName:
          type: ["string", "null"]
        projectName:
          type: ["string", "null"]
        crmOpportunityNumber:
          type: ["string", "null"]
        bmcdContractValueRaw:
          type: ["string", "null"]
        totalInstalledCostRaw:
          type: ["string", "null"]
        businessLines:
          type: array
          items:
            type: string
        contractReviewRvwNumber:
          type: ["string", "null"]
        isEpcPrime:
          type: boolean
        requestType:
          type: ["string", "null"]
        riskIdentificationStatus:
          type: ["string", "null"]
        preRiskTargetDate:
          type: ["string", "null"]
        formalRiskTargetDate:
          type: ["string", "null"]
        proposalDueDate:
          type: ["string", "null"]
        formalRiskDiscussionDate:
          type: ["string", "null"]
        finalRiskTargetDate:
          type: ["string", "null"]
        preRiskLead:
          type: ["string", "null"]
        formalRiskLead:
          type: ["string", "null"]
        status:
          type: ["string", "null"]
        nextAction:
          type: ["string", "null"]
        owner:
          type: ["string", "null"]
        notes:
          type: ["string", "null"]
        triggerIds:
          type: array
          items:
            type: integer
        attendees:
          type: array
          items:
            $ref: "#/components/schemas/AttendeeInput"

    StatusChangeInput:
      type: object
      required:
        - newStatus
      properties:
        newStatus:
          type: string
        nextAction:
          type: ["string", "null"]
        changedBy:
          type: ["string", "null"]
        notes:
          type: ["string", "null"]

    CalendarPreviewInput:
      type: object
      properties:
        meetingId:
          type: ["integer", "null"]
        meetingType:
          type: ["string", "null"]

    CalendarPreview:
      type: object
      required:
        - subject
        - body
        - timezone
      properties:
        subject:
          type: string
        body:
          type: string
        requiredAttendees:
          type: array
          items:
            type: string
        optionalAttendees:
          type: array
          items:
            type: string
        start:
          type: ["string", "null"]
        end:
          type: ["string", "null"]
        timezone:
          type: string
        showAs:
          type: string
        responseRequested:
          type: boolean
        reminderMinutes:
          type: integer
        teamsLink:
          type: string

    AppConfig:
      type: object
      required:
        - statuses
        - nextActions
        - businessLines
        - meetingTypes
        - meetingStatuses
        - attendeeRoles
        - epcPrimeRoles
        - standardRoles
        - requiredAttendeeRoles
        - requestTypes
        - riskIdentificationStatuses
        - draftStatuses
        - templateTypes
      properties:
        statuses:
          type: array
          items:
            type: string
        nextActions:
          type: array
          items:
            type: string
        businessLines:
          type: array
          items:
            type: string
        meetingTypes:
          type: array
          items:
            type: string
        meetingStatuses:
          type: array
          items:
            type: string
        attendeeRoles:
          type: array
          items:
            type: string
        epcPrimeRoles:
          type: array
          items:
            type: string
        standardRoles:
          type: array
          items:
            type: string
        requiredAttendeeRoles:
          type: array
          items:
            type: string
        requestTypes:
          type: array
          items:
            type: string
        riskIdentificationStatuses:
          type: array
          items:
            type: string
        draftStatuses:
          type: array
          items:
            type: string
        templateTypes:
          type: array
          items:
            type: string

    DashboardSummary:
      type: object
      required:
        - newRequests
        - readyToSchedule
        - preRiskScheduled
        - formalRiskScheduled
        - finalRiskScheduled
        - missingInfo
        - completed
        - total
      properties:
        newRequests:
          type: integer
        readyToSchedule:
          type: integer
        preRiskScheduled:
          type: integer
        formalRiskScheduled:
          type: integer
        finalRiskScheduled:
          type: integer
        missingInfo:
          type: integer
        completed:
          type: integer
        total:
          type: integer

    UsageEvent:
      type: object
      required:
        - id
        - program
        - usage
        - action
        - usageUnit
        - minutesPerUnit
        - minutesSaved
        - hoursSaved
        - source
        - forwardStatus
      properties:
        id:
          type: integer
        program:
          type: string
        addin:
          type: ["string", "null"]
        version:
          type: ["string", "null"]
        usage:
          type: string
        action:
          type: string
        username:
          type: ["string", "null"]
        usageUnit:
          type: integer
        minutesPerUnit:
          type: integer
        minutesSaved:
          type: integer
        hoursSaved:
          type: number
        entityType:
          type: ["string", "null"]
        entityId:
          type: ["integer", "null"]
        source:
          type: string
        forwardStatus:
          type: string
        forwardError:
          type: ["string", "null"]
        detail:
          type: ["string", "null"]
        createdAt:
          type: ["string", "null"]

    RecordUsageBody:
      type: object
      properties:
        program:
          type: string
        addin:
          type: string
        version:
          type: string
        usage:
          type: string
        action:
          type: string
        username:
          type: string
        usageUnit:
          type: integer
        minutesPerUnit:
          type: integer
        source:
          type: string
        detail:
          type: string

    UsageActionSummary:
      type: object
      required:
        - action
        - label
        - count
        - usageUnits
        - minutesSaved
        - hoursSaved
        - dollarsSaved
      properties:
        action:
          type: string
        label:
          type: string
        count:
          type: integer
        usageUnits:
          type: integer
        minutesSaved:
          type: integer
        hoursSaved:
          type: number
        dollarsSaved:
          type: number

    UsageMonthSummary:
      type: object
      required:
        - month
        - count
        - minutesSaved
        - hoursSaved
        - dollarsSaved
      properties:
        month:
          type: string
        count:
          type: integer
        minutesSaved:
          type: integer
        hoursSaved:
          type: number
        dollarsSaved:
          type: number

    UsageSummary:
      type: object
      required:
        - rate
        - totalEvents
        - totalUsageUnits
        - totalMinutesSaved
        - totalHoursSaved
        - totalDollarsSaved
        - byAction
        - byMonth
      properties:
        rate:
          type: number
        totalEvents:
          type: integer
        totalUsageUnits:
          type: integer
        totalMinutesSaved:
          type: integer
        totalHoursSaved:
          type: number
        totalDollarsSaved:
          type: number
        byAction:
          type: array
          items:
            $ref: "#/components/schemas/UsageActionSummary"
        byMonth:
          type: array
          items:
            $ref: "#/components/schemas/UsageMonthSummary"

    ImportOutcome:
      type: object
      required:
        - rowNumber
        - label
        - result
      properties:
        rowNumber:
          type: integer
        label:
          type: string
        result:
          type: string
          enum:
            - imported
            - skipped
            - error
        reason:
          type: string

    ImportSummary:
      type: object
      required:
        - sourceFile
        - dryRun
        - rowsRead
        - imported
        - skipped
        - errored
        - outcomes
      properties:
        sourceFile:
          type: string
        dryRun:
          type: boolean
        rowsRead:
          type: integer
        imported:
          type: integer
        skipped:
          type: integer
        errored:
          type: integer
        outcomes:
          type: array
          items:
            $ref: "#/components/schemas/ImportOutcome"

    TrackerUpload:
      type: object
      required:
        - file
      properties:
        file:
          type: string
          format: binary
          description: The tracker spreadsheet (.xlsx, .xls, or .csv).
